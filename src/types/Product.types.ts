

export type ProductProps = {
    images: string[] | string | any,
    category: string[],
    title:string,
    price:number,
    id:string,
    short: string,
    colors: string[],
    sizes: string[]
}